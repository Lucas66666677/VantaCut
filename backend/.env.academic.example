# Academic mode uses provider abstraction already configured for text/ASR.
AI_TEXT_PROVIDER=gemini
AI_ASR_PROVIDER=openai_whisper
# Conservative default; 0.96 means a 4% globally A/V-synchronised slow-down.
# Configured per Timeline in POST /academic-mode, permitted range 0.90-1.03.
